{
  "name": "cv-personal",
  "version": "1.0.0",
  "description": "",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "deploy": "astro build && wrangler deploy",
    "release:patch": "pnpm version patch && git push --follow-tags && pnpm run deploy",
    "release:minor": "pnpm version minor && git push --follow-tags && pnpm run deploy",
    "release:major": "pnpm version major && git push --follow-tags && pnpm run deploy"
  },
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^7.2.9",
    "tailwindcss": "^3.4.19",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  },
  "private": true
}
